{
  "name": "@my-scope/db",
  "version": "0.0.0",
  "private": true,
  "main": ".dist/index.js",
  "types": ".dist/index.d.ts",
  "files": [
    ".dist"
  ],
  "exports": {
    ".": {
      "types": "./.dist/index.d.ts",
      "require": "./.dist/index.js",
      "import": "./.dist/index.mjs"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --out-dir .dist --clean",
    "dev": "tsup src/index.ts --format cjs,esm --dts --out-dir .dist --watch",
    "start": "echo 'No start needed'",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .dist .turbo",
    "db:test": "dotenv -e ../../.env -- bun run scripts/test-connection.ts"
  },
  "dependencies": {
    "@my-scope/shared": "*",
    "mongoose": "^8.5.2",
    "bson": "^6.10.4"
  },
  "devDependencies": {
    "@my-scope/eslint-config": "*",
    "@my-scope/typescript-config": "*",
    "@types/node": "^20.14.0",
    "eslint": "^8.57.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  }
}
